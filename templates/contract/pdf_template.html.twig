<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Expense Report</title>
		<style>
			body {
				font-family: Arial, sans-serif;
				background-color: #f8f9fa;
				color: #333;
			}

			.container {
				width: 80%;
				margin: 50px auto;
				padding: 20px;
				border-radius: 10px;
				background-color: #fff;
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
			}

			.header {
				text-align: center;
				margin-bottom: 20px;
			}

			.logo {
				width: 120px;
				height: auto;
				margin-bottom: 20px;
			}

			h1 {
				color: rgba(54, 162, 235, 0.2);
			}

			table {
				width: 100%;
				border-collapse: collapse;
				margin-bottom: 20px;
			}

			th,
			td {
				padding: 12px 15px;
				text-align: left;
				border-bottom: 1px solid #ddd;
			}

			th {
				background-color: #f2f2f2;
			}

			tr:nth-child(even) {
				background-color: #f2f2f2;
			}

			.facture-total {
				text-align: right;
				margin-top: 20px;
				font-weight: bold;
				border-top: 2px solid rgba(54, 162, 235, 0.2);
				padding-top: 10px;
			}

			.facture-total p {
				margin: 0;
			}

			.footer {
				text-align: center;
				margin-top: 20px;
				font-size: 12px;
				color: #666;
			}

			.signature {
				text-align: right;
				margin-top: 50px;
				font-style: italic;
				color: #888;
			}

			/* Increase spacer height */
			.spacer {
				height: 100px; /* Adjust the height as needed */
			}
		</style>
	</head>
	<body>
		<div class="container">
			<div class="header">
				<img src="{{ asset('/marketPlaceImages/download.jpg') }}" alt="main_logo" class="logo">
				<div style="height:1000px"></div>
				<h1>Contract Report</h1>
				<div style="height:1000px"></div>

				<p>Date:
					{{ transaction.contract.terminationDate|date("d/m/Y") }}</p>
			</div>
			<div class="facture-details">
				<div style="height:1000px"></div>

				<table>
					<tr>
						<th>Title: </th>
						<th>Party A ID:</th>
						<th>Party B ID:</th>
						<th>Item Name: </th>
						<th>Effective Date: </th>
						<th>Termination Date: </th>
						<th>Terms and Conditions: </th>
						<th>Payment Method: </th>
					</tr>
					<tr>

						<td>Sale contract</td>
						<td>{{ transaction.idSeller }}</td>
						<td>{{ transaction.idBuyer }}</td>
						<td>{{ transaction.product.name }}</td>
						<td>{{ transaction.contract.effectiveDate|date("d/m/Y") }}</td>
						<td>{{ transaction.contract.terminationDate|date("d/m/Y") }}</td>
						<td>{{ transaction.contract.termsAndConditions }}</td>
						<td>{{ transaction.contract.paymentMethod }}</td>
					</tr>
					<!-- Add more rows as needed -->
				</table>
			</div>
			<div class="facture-total">
				<p>Total Amount: 5000</p>
			</div>
		</div>

	</body>
</html>
